# LaTeX Paper Draft

This directory stores the LaTeX version of the SCP-Flow paper draft.

Current layout:

- `main.tex`: main entry
- `sections/`: section files
- `tables/`: table snippets
- `figures/`: figure assets
- `references.bib`: bibliography placeholder

Recommended next steps:

1. Replace placeholder references in `references.bib`
2. Fill real results into `tables/`
3. Expand `sections/methods.tex` to the final paper version
4. Add figure assets under `figures/`
